const { Sequelize } = require('sequelize');

const doctorModel = require('./models/Doctor');
const userModel = require('./models/User');
const planModel = require('./models/Plan');

// Falta configurar con base de datos hosteada